server:
  port: 8080

spring:
  application:
    name: customer
  datasource:
    username: 'root'
    url: jdbc:postgresql://localhost:5432/customer
    password: 'admin'
    jpa:
      properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgresSQLDialect
          format_sql: 'true'
      hibernate:
        ddl-auto: update
      show-sql: 'true'
